# abstraemos atributos y comportamientos que compartan las clases alcanzadas a una clase 'super()'
# abstraccion & herencia
# identificar patron composite

# implementamos metodos, tanto en clase simples como compuestas 
(que puedan contener atributos con mas instancias de la misma familia dentro)
# Blinding Dinamica, se ejecutara un metodo u otro de acuerdo al TIPO especifico del elemento
# con el que se haya llamado 
(si es de una clase simple, compuesta u otra clase hija especifica donde tmb se haya implementado el metodo)

# identificamos que operaciones debemos realizar, como retornarlo y deacuerdo 
# a si es por criterio de filtrado, comparacion y/o a igualacion entre objetos

# criterios/estrategias - interface , comparable || comparator - interface , igualacion o pertenecia - equals
